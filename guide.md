# react native app
```
$ sudo npm i -g create-react-native-app # to install cli
$ create-react-native-app <project_name> # to create a new app
$ cd <project_name>
$ npm start # to bootstrap the project
$ npm install react-native-elements --save  # add material design
```